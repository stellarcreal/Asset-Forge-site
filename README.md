# Asset Forge Site

This repository hosts the published static website for Asset Forge.

Deployment model:
- GitHub Pages from the `main` branch root
- prebuilt MkDocs output committed directly to the repository
- `.nojekyll` kept at the root so Pages serves static assets without Jekyll processing
